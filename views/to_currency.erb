<h1> Convert <%= @original_currency %> to <%= @destination_currency %>  </h1>

<%#= @parsed_data.class %>
<!--parsed_data is a hash and when made as an @, and when you add it here you can see everything you can fetch with it, one of which is the fetch method [] and you can see that "result" gives you the currency_value_result-->

<p> 1 <%= @original_currency %> equals  <%= @currency_value_results %>  <%= @destination_currency %> 
</p>

<p> Go <a href="/<%= @original_currency %>">back. </a> </p>
